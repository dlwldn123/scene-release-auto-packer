# Core dependencies
ebookmeta>=1.0.0
ebookatty>=0.2.0
PyPDF2>=3.0.0
python-docx>=1.1.0  # Pour extraction métadonnées DOCX
PyYAML>=6.0
Jinja2>=3.1.6  # Security updates
MarkupSafe>=3.0.3  # Security updates
pillow>=10.0.0  # Pour manipulation images CBZ/PDF
requests>=2.32.5  # Pour APIs d'enrichissement
beautifulsoup4>=4.12.0  # Pour parsing HTML scenerules.org
psutil>=5.9.0  # Utilitaires optionnels (process detection)

# Web interface
flask>=2.3.0
flask-cors>=4.0.0
flask-caching>=2.3.0
gunicorn>=21.2.0  # Pour production Docker
flask-sqlalchemy>=3.0.0
flask-migrate>=4.0.0
flask-jwt-extended>=4.6.0
flask-compress>=1.15
marshmallow>=3.20.0
marshmallow-sqlalchemy>=0.29.0
pymysql>=1.1.0
cryptography>=41.0.0  # Pour chiffrement API keys
paramiko>=3.3.0  # Pour SFTP

# Testing (dépendances optionnelles mais recommandées)
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.0

# Build tools (security fix)
setuptools>=78.1.1  # CVE PYSEC-2025-49 fix
attrs>=25.4.0  # Security updates
certifi>=2025.10.5  # Security updates
urllib3>=2.5.0  # Security updates

# Optional but recommended (installer séparément)
# calibre - Installer via le gestionnaire de paquets système:
#   sudo apt-get install calibre  # Debian/Ubuntu
#   brew install calibre          # macOS
