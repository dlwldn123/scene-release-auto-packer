/** New release page component. */

import { PageLayout } from '../components/PageLayout';

/**
 * New release page component (placeholder).
 */
export function NewRelease() {
  return (
    <PageLayout title="Nouvelle Release" description="Créer une nouvelle release">
      <p>Cette page sera implémentée dans la Phase 3 (Wizard 9 étapes).</p>
    </PageLayout>
  );
}
