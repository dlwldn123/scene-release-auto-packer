/** Rules page component. */

import { PageLayout } from '../components/PageLayout';

/**
 * Rules page component (placeholder).
 */
export function Rules() {
  return (
    <PageLayout title="Rules" description="Gérer les règles Scene">
      <p>Cette page sera implémentée dans la Phase 5.</p>
    </PageLayout>
  );
}
